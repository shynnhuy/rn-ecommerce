import React from "react";
import { StyleSheet, Text, View } from "react-native";

export const ListEmpty = () => {
  return (
    <View>
      <Text>Not found any products</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
